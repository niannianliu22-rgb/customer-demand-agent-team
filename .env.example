# Customer Demand Agent Team uses authenticated Codex CLI and Claude CLI sessions.
# No API key is read by the supported Runtime. Usually leave this file unchanged.

# Optional development/test override for the model-runtime binding configuration.
# CDAT_RUNTIME_BINDING_CONFIG=config/models/model_runtime_binding_v1.yaml
